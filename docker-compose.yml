Lệnh nào được sử dụng để xây dựng Docker image từ Dockerfile?	docker build -t my-mongo-image .	docker run -d --name my-mongo-container	docker-compose up	docker image ls	1	Lệnh docker build -t my-mongo-image . được sử dụng để xây dựng Docker image từ Dockerfile.
Lệnh nào được sử dụng để chạy container từ image đã tạo?	docker ps	docker run -d --name my-mongo-container	docker stop	docker logs	2	Lệnh docker run -d --name my-mongo-container được sử dụng để chạy container từ image đã tạo.
Biến môi trường nào được thiết lập để tạo tài khoản quản trị cho MongoDB?	MONGO_INITDB_ROOT_USERNAME	MONGO_INITDB_ROOT_PASSWORD	Cả hai trên	Không biến nào	3	Cả hai biến môi trường MONGO_INITDB_ROOT_USERNAME và MONGO_INITDB_ROOT_PASSWORD được thiết lập để tạo tài khoản quản trị cho MongoDB.
Lệnh nào mở cổng mặc định của MongoDB trong Dockerfile?	EXPOSE 8080	EXPOSE 27017	EXPOSE 3306	EXPOSE 5432	2	Lệnh EXPOSE 27017 trong Dockerfile mở cổng mặc định của MongoDB để có thể truy cập từ bên ngoài.
Thư mục nào được chỉ định để lưu trữ dữ liệu MongoDB trong Dockerfile?	/var/lib/mongo	/data/db	/mongo/data	/usr/local/mongo	2	Thư mục /data/db được chỉ định trong Dockerfile để lưu trữ dữ liệu của MongoDB.
Cổng mặc định của MongoDB là gì?	8080	27017	3306	5432	2	Cổng mặc định của MongoDB là 27017.
Lệnh nào được sử dụng để kết nối tới MongoDB qua dòng lệnh?	mongo	mongosh	mongo-cli	mongotool	2	Lệnh mongosh được sử dụng để kết nối tới MongoDB qua dòng lệnh.
Tham số nào trong lệnh mongosh chỉ định địa chỉ máy chủ?	--port	--host	--authenticationDatabase	--username	2	Tham số --host trong lệnh mongosh chỉ định địa chỉ máy chủ.
Tham số nào trong lệnh mongosh chỉ định cơ sở dữ liệu xác thực?	--db	--auth	--authenticationDatabase	--database	3	Tham số --authenticationDatabase trong lệnh mongosh chỉ định cơ sở dữ liệu xác thực.
Tên của image MongoDB được sử dụng trong Dockerfile là gì?	mongo:latest	mongo:stable	mongodb:latest	mongodb:stable	1	Tên của image MongoDB được sử dụng trong Dockerfile là mongo:latest.
Tham số nào trong lệnh docker run ánh xạ cổng trên máy chủ với cổng trong container?	-d	-p	--name	--volume	2	Tham số -p trong lệnh docker run ánh xạ cổng trên máy chủ với cổng trong container.
Lệnh nào hiển thị danh sách các container đang chạy?	docker ps	docker images	docker stop	docker exec	1	Lệnh docker ps hiển thị danh sách các container đang chạy.
Tên container được đặt trong lệnh docker run là gì?	my-mongo-image	my-mongo-container	mongo-container	mongo-image	2	Tên container được đặt trong lệnh docker run là my-mongo-container.
Tại sao cần sử dụng lệnh EXPOSE trong Dockerfile?	Để đặt biến môi trường	Để mở cổng mặc định của ứng dụng	Để tạo thư mục lưu trữ dữ liệu	Để xây dựng image	2	Lệnh EXPOSE được sử dụng để mở cổng mặc định của ứng dụng (ở đây là MongoDB) để có thể truy cập từ bên ngoài.
Biến môi trường nào chứa mật khẩu của tài khoản quản trị MongoDB?	MONGO_INITDB_ROOT_USERNAME	MONGO_INITDB_ROOT_PASSWORD	MONGO_PASSWORD	ROOT_PASSWORD	2	Biến môi trường MONGO_INITDB_ROOT_PASSWORD chứa mật khẩu của tài khoản quản trị MongoDB.
